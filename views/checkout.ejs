<%- include('partials/header') %>

<div class="container">
  <div class="order-section">
    <h2>Resumen de tu pedido #<%= order.id %></h2>

    <table class="checkout-table">
      <thead>
        <tr>
          <th>Producto</th>
          <th>Cantidad</th>
          <th>Precio unit.</th>
          <th>Subtotal</th>
        </tr>
      </thead>
      <tbody>
        <% items.forEach(item => { %>
          <tr>
            <td><%= item.product %></td>
            <td><%= item.quantity %> kg</td>
            <td><%= formatPrice(item.unit_price) %>/kg</td>
            <td><%= formatPrice(item.quantity * item.unit_price) %></td>
          </tr>
        <% }) %>
        <% if (order.shipping_cost > 0) { %>
          <tr>
            <td>Envío</td>
            <td><%= order.shipping_distance_km ? Math.round(order.shipping_distance_km) + ' km' : '' %></td>
            <td></td>
            <td><%= formatPrice(order.shipping_cost) %></td>
          </tr>
        <% } %>
        <tr class="total-row">
          <td colspan="3">Total</td>
          <td><%= formatPrice(order.total_amount) %></td>
        </tr>
      </tbody>
    </table>

    <div class="customer-summary">
      <p><strong>Nombre:</strong> <%= order.customer_name %></p>
      <p><strong>Teléfono:</strong> <%= order.customer_phone %></p>
      <% if (order.customer_email) { %>
        <p><strong>Email:</strong> <%= order.customer_email %></p>
      <% } %>
      <p><strong>Dirección:</strong> <%= order.customer_address %></p>
      <p><strong>Entrega:</strong> <%= order.delivery_day === 'miercoles' ? 'Miércoles' : 'Sábado' %><% if (order.delivery_slot) { %> — <%= order.delivery_slot === 'manana' ? 'Mañana (10 a 13hs)' : order.delivery_slot === 'tarde' ? 'Tarde (13 a 18hs)' : 'Noche (18 a 20hs)' %><% } %></p>
    </div>

    <a href="/payment/create/<%= order.id %>" class="btn btn-mp">Pagar con MercadoPago</a>
  </div>
</div>

<%- include('partials/footer') %>
